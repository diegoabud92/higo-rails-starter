<%= form_with url: upload_invoices_invoices_path, method: :post, local: true, html: { multipart: true } do %>
  <div class="form-group">
    <%= file_field_tag :file, class: 'form-control' %>
  </div>
  <%= submit_tag "Upload", class: "btn btn-primary" %>
<% end %>